<UserControl x:Class="WPF.Controls.Items.ItemListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Controls.Items"
             xmlns:items="clr-namespace:WPF.Controls.Items"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <ListBox ItemsSource="{Binding ItemListingItemViewModels}"
                 SelectionMode="Single"
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <items:ItemListingItemView />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
